spring:
  application:
    name: gateway-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true
      retry:
        max-attempts: 5
        initial-interval: 3000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
